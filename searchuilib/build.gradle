apply plugin: 'com.android.library'

android {
    namespace "com.android.app.search"
    sourceSets {
        main {
            java.srcDirs = ['src']
            kotlin.directories.add('src')
        }
    }
}
